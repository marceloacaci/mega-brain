---
tags: [stub, placeholder]
tipo: stub
---

# nome

Nota-ponte (antes tinha **0 bytes** e era reportada por `/validate` como `nota_vazia`).

Criada por acidente a partir do **placeholder** `[[nome]]` que aparece em
[[10_MEGA_BRAIN/PROMPT_MESTRE_v2|PROMPT MESTRE v2]] — ali `[[nome]]` é um exemplo
genérico ("o padrão *tal*"), não uma nota real. Esse placeholder já foi convertido em
código inline no prompt para não gerar mais wikilink.

> Mantida (em vez de apagada) para não quebrar nenhum link histórico. Pode ser removida
> com segurança se `/backlinks?path=nome.md` indicar zero entradas.
